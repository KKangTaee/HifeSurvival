public enum EGameModeStatus
{
    None,
    Ready,
    Countdown,
    LoadGame,
    PlayStart,
    FinishGame,
}

public enum ERewardStatus
{
    NONE          = 0,
    DROP_REWARD   = 1,
    PICK_REWARD   = 2,
}

public enum EStatType
{
    NONE = 0,

    STR,

    DEF,

    HP,
}

public enum ECurrency
{
    NONE = 0,
    GOLD,
}